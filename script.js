var animation = bodymovin.loadAnimation({
  container: document.getElementById("animContainer"),
  renderer: "svg",
  loop: false,
  autoplay: true,
  path: "/assect/success.json", // lottie file path
});
